<template>
  <section class="bg-white py-16">
    <div class="max-w-6xl mx-auto px-6">
      <!-- Título -->
      <div class="text-center mb-12">
        <h2 class="text-2xl md:text-3xl font-bold text-slate-900 mb-3">
          Oficinas reais já estão usando o Augile
        </h2>
        <p class="text-slate-600 text-lg">
          Uma plataforma em crescimento, construída junto com quem vive a oficina.
        </p>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 text-center">
        <!-- Oficinas -->
        <div class="flex flex-col items-center">
          <div
            class="w-14 h-14 rounded-xl bg-blue-100 text-blue-600 flex items-center justify-center text-2xl mb-4"
          >
            🚗
          </div>
          <p class="text-xl font-bold text-slate-900">
            Oficinas ativas
          </p>
          <p class="text-slate-600 text-sm mt-1">
            utilizando o Augile no dia a dia
          </p>
        </div>

        <!-- Municípios -->
        <div class="flex flex-col items-center">
          <div
            class="w-14 h-14 rounded-xl bg-green-100 text-green-600 flex items-center justify-center text-2xl mb-4"
          >
            📍
          </div>
          <p class="text-xl font-bold text-slate-900">
            Presente em municípios
          </p>
          <p class="text-slate-600 text-sm mt-1">
            de diferentes regiões
          </p>
        </div>

        <!-- Clientes / OS -->
        <div class="flex flex-col items-center">
          <div
            class="w-14 h-14 rounded-xl bg-purple-100 text-purple-600 flex items-center justify-center text-2xl mb-4"
          >
            👥
          </div>
          <p class="text-xl font-bold text-slate-900">
            Ordens de serviço
          </p>
          <p class="text-slate-600 text-sm mt-1">
            organizadas na plataforma
          </p>
        </div>
      </div>
    </div>
  </section>
</template>
