<template>
  <section class="bg-slate-900 py-20">
    <div class="max-w-5xl mx-auto px-6 text-center text-white">
      <!-- Título -->
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        Comece sem risco. Cancele quando quiser.
      </h2>

      <!-- Subtítulo -->
      <p class="text-slate-300 text-lg mb-10">
        Use o plano gratuito para sempre ou teste o plano básico por 14 dias.
        Sem cartão de crédito. Sem fidelidade.
      </p>

      <!-- Benefícios rápidos -->
      <div class="flex flex-col sm:flex-row justify-center gap-6 mb-12 text-slate-200">
        <div class="flex items-center gap-2">
          <span class="text-green-400">✔</span>
          Plano gratuito para sempre
        </div>
        <div class="flex items-center gap-2">
          <span class="text-green-400">✔</span>
          Teste de 14 dias no plano básico
        </div>
        <div class="flex items-center gap-2">
          <span class="text-green-400">✔</span>
          Acesso seguro via WhatsApp
        </div>
      </div>

      <!-- CTA -->
      <div class="flex flex-col sm:flex-row justify-center gap-4">
        <button
          class="px-8 py-4 rounded-lg font-semibold bg-white text-slate-900 hover:bg-slate-100 transition"
        >
          Começar grátis agora
        </button>

        <button
          class="px-8 py-4 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition"
        >
          Testar plano básico
        </button>
      </div>

      <!-- Microcopy final -->
      <p class="mt-6 text-sm text-slate-400">
        🔒 Usamos seu CPF para identificação e enviamos o acesso pelo WhatsApp.
      </p>
    </div>
  </section>
</template>
